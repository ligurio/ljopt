local lj_dump = require("luajit_dump")
local lj_bc = require("luajit_bc")

local function write()
end

local function new()
    local self = {
        dump = {},
    }
    return setmetatable(t,)
end
